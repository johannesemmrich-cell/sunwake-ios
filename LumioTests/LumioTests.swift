import XCTest
@testable import Lumio

final class LumioTests: XCTestCase {
    func testCalendarEventEquality() throws {
        // Placeholder: add real unit tests as features are built
        XCTAssertTrue(true)
    }
}
